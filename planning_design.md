# 플로깅 앱 기획/설계 문서 초안

## 1. 문서 목적

본 문서는 플로깅 앱 개발을 시작하기 전, 서비스의 핵심 기능, 화면 흐름, 사용자 권한, 데이터베이스 구조, API 목록을 정리하기 위한 기획/설계 문서이다.

이 앱은 Flutter 기반 프론트엔드와 백엔드 서버, PostgreSQL 데이터베이스를 분리하여 개발하는 구조를 전제로 한다.

---

## 2. 서비스 한 줄 정의

> 위치 기반으로 개인 및 단체 플로깅 활동을 기록하고, 수거한 쓰레기 데이터를 지역별·사용자별 통계로 시각화하는 환경 활동 플랫폼

---



> 비로그인 사용자는 조회만 가능하고, 기록·참여·작성은 로그인 필요로 제한한다.

즉, 비로그인 사용자는 홈 화면, 일정 확인, 지역 검색, 단체 플로깅 목록 조회 정도는 가능하지만, 실제 플로깅 시작, 단체 참여, 글쓰기, 댓글 작성, 좋아요, 마이페이지 접근은 불가능하게 한다.

필수 입력:

- 아이디
- 비밀번호
- 이름
- 전화번호
- 닉네임
- 지역

선택 입력:

- 프로필 사진
- 생년월일

#### 3) 전화번호 입력과 본인 인증이 중복될 수 있음

본인 인증을 전화번호 인증으로 처리한다면, 인증된 전화번호를 회원 정보에 자동 저장하는 구조가 좋다.

흐름은 다음과 같다.

> 전화번호 입력 → 인증번호 확인 → 인증 성공 → 해당 전화번호를 회원 정보에 저장

#### 4) 카카오 로그인 후 추가 정보 입력 흐름이 필요함

카카오 로그인은 일반 회원가입과 다르다. 카카오 계정에서 앱에 필요한 모든 정보를 받지 못할 수 있다.

따라서 카카오 최초 로그인 사용자는 추가 정보 입력 화면을 거쳐야 한다.

필요한 추가 정보:

- 닉네임
- 지역
- 필수 약관 동의
- 전화번호 인증 여부


#### 6) 지도에서 타인의 실시간 위치를 보여주면 안 됨

지도에 플로깅 현황을 표시할 때 다른 사용자의 실시간 위치를 보여주면 개인정보 문제가 발생할 수 있다.

지도에 표시해도 되는 정보:

- 내 현재 위치
- 내 플로깅 경로
- 단체 플로깅 모임 장소
- 쓰레기 등록 위치
- 분리수거장 위치
- 공공 쓰레기통 위치

지도에 표시하면 안 되는 정보:

- 다른 사용자의 현재 위치
- 다른 사용자의 실시간 이동 경로
- 특정 사용자의 정확한 활동 패턴

---

## 4. 전체 서비스 구조

### 4.1 사용자 유형

| 사용자 유형 | 설명 |
|---|---|
| 비회원 | 홈, 일정, 지역 검색, 단체 플로깅 목록 조회 가능 |
| 일반 회원 | 개인 플로깅, 단체 참여, 커뮤니티 작성, 활동 기록 확인 가능 |
| 모임 인솔자 | 단체 플로깅 생성, 수정, 취소, 참여자 관리 가능 |
| 관리자 | 공지사항, 신고, 통계, 사용자 관리 가능 |

---

## 5. 핵심 기능 우선순위

### 5.1 1순위 기능: MVP 필수 기능

초기 버전에서 반드시 구현해야 하는 기능이다.

1. 회원가입 및 로그인
2. 자동 로그인
3. 홈 화면
4. 개인 플로깅 시작 및 종료
5. 위치 기반 지도 표시
6. 쓰레기 등록
7. 내 활동 기록 확인
8. 기본 데이터 저장

### 5.2 2순위 기능: 서비스 완성도 기능

MVP 이후 구현하면 좋은 기능이다.

1. 단체 플로깅 목록
2. 단체 플로깅 생성
3. 단체 플로깅 참여
4. 단체 플로깅 상세 화면
5. 커뮤니티 글 작성
6. 댓글 및 좋아요

### 5.3 3순위 기능: 확장 기능

프로젝트 완성도와 차별성을 높이는 기능이다.

1. 데이터 시각화
2. 지역별 쓰레기 통계
3. 분리수거장 위치 확인
4. 공지사항
5. 알림
6. 실시간 채팅
7. AI 쓰레기 분류

---

## 6. 앱 전체 화면 구조

하단 네비게이션은 다음 5개로 구성한다.

> 홈 / 개인 플로깅 / 단체 플로깅 / 지도 / 커뮤니티

마이페이지는 하단 네비게이션에 넣지 않고, 홈 화면 우측 상단의 프로필 아이콘 또는 사이드바를 통해 접근한다.

### 6.1 홈 화면

홈 화면은 앱의 시작 화면이다.

주요 구성 요소:

- 오늘의 플로깅
- 플로깅 일정
- 지역 검색
- 현재 내 위치 기반 추천
- 주변 단체 플로깅 미리보기
- 개인 플로깅 시작 바로가기
- 최근 커뮤니티 글
- 로그인/프로필 버튼

비로그인 사용자는 홈 화면을 볼 수 있지만, 개인 플로깅 시작이나 단체 참여 버튼을 누르면 로그인 화면으로 이동한다.

### 6.2 개인 플로깅 화면

개인 플로깅 화면은 혼자 플로깅을 시작하고 기록하는 화면이다.

주요 구성 요소:

- 현재 위치 지도
- 플로깅 시작 버튼
- 활동 시간
- 이동 거리
- 쓰레기 등록 버튼
- 일시정지 버튼
- 종료 버튼

### 6.3 단체 플로깅 화면

단체 플로깅 화면은 모임을 검색하거나 생성하고 참여하는 화면이다.

주요 구성 요소:

- 단체 플로깅 목록
- 지역 검색
- 날짜 검색
- 모집 상태 필터
- 단체 플로깅 상세 보기
- 단체 플로깅 생성 버튼

단체 플로깅 생성 항목:

- 활동명
- 지역
- 일정
- 시작 시간
- 종료 시간
- 모집 인원
- 인솔자
- 출발지
- 상세 설명
- 준비물
- 모집 마감 시간

### 6.4 지도 화면

지도 화면은 플로깅 관련 위치 정보를 확인하는 화면이다.

표시 정보:

- 내 현재 위치
- 내 플로깅 경로
- 단체 플로깅 모임 장소
- 쓰레기 등록 위치
- 분리수거장 위치
- 공공 쓰레기통 위치

지도 필터:

- 전체
- 단체 플로깅
- 쓰레기 등록 위치
- 분리수거장
- 공공 쓰레기통

### 6.5 커뮤니티 화면

커뮤니티 화면은 플로깅 활동을 공유하는 공간이다.

주요 기능:

- 게시글 목록
- 게시글 상세
- 글 작성
- 사진 첨부
- 댓글
- 좋아요
- 신고
- 글 수정
- 글 삭제

정확한 위치 정보는 공개하지 않고, 지역 단위로만 표시한다.

예시:

- 수원시 영통구
- 아주대 근처
- 서울시 마포구

### 6.6 마이페이지

마이페이지는 사용자 개인 정보와 활동 기록을 확인하는 화면이다.

주요 구성 요소:

- 이름
- 연락처
- 생년월일
- 닉네임
- 지역
- 프로필 사진
- 내 활동 기록
- 내가 만든 단체 플로깅
- 내가 참여한 단체 플로깅
- 내가 쓴 커뮤니티 글
- 내 통계
- 공지사항
- 로그아웃

---

## 7. 사용자 접근 권한 정책

### 7.1 비로그인 사용자

| 기능 | 가능 여부 |
|---|---|
| 홈 화면 조회 | 가능 |
| 오늘의 플로깅 조회 | 가능 |
| 플로깅 일정 조회 | 가능 |
| 지역 검색 | 가능 |
| 단체 플로깅 목록 조회 | 가능 |
| 단체 플로깅 상세 조회 | 가능 |
| 개인 플로깅 시작 | 불가 |
| 단체 플로깅 참여 | 불가 |
| 단체 플로깅 생성 | 불가 |
| 쓰레기 등록 | 불가 |
| 커뮤니티 글 조회 | 선택 가능 |
| 커뮤니티 글쓰기 | 불가 |
| 댓글/좋아요 | 불가 |
| 마이페이지 | 불가 |

### 7.2 로그인 사용자

| 기능 | 가능 여부 |
|---|---|
| 개인 플로깅 시작 | 가능 |
| 쓰레기 등록 | 가능 |
| 단체 플로깅 참여 | 가능 |
| 단체 플로깅 생성 | 가능 |
| 커뮤니티 글 작성 | 가능 |
| 댓글/좋아요 | 가능 |
| 내 활동 기록 확인 | 가능 |
| 내 통계 확인 | 가능 |

### 7.3 모임 인솔자

| 기능 | 가능 여부 |
|---|---|
| 본인이 만든 단체 플로깅 수정 | 가능 |
| 본인이 만든 단체 플로깅 취소 | 가능 |
| 참여자 목록 확인 | 가능 |
| 참여자 출석 처리 | 가능 |

### 7.4 관리자

| 기능 | 가능 여부 |
|---|---|
| 공지사항 작성 | 가능 |
| 신고 게시글 관리 | 가능 |
| 사용자 관리 | 가능 |
| 전체 통계 조회 | 가능 |
| 분리수거장 위치 데이터 관리 | 가능 |

---

## 8. 주요 사용자 흐름

### 8.1 앱 시작 흐름

```text
앱 실행
→ 저장된 토큰 확인
→ 토큰 유효함: 로그인 상태로 홈 화면 이동
→ 토큰 없음 또는 만료: 비로그인 홈 화면 이동
→ 로그인 필요한 기능 클릭 시 로그인 화면 이동
```

### 8.2 일반 회원가입 흐름

```text
회원가입 클릭
→ 약관 동의
→ 전화번호 본인 인증
→ 기본 정보 입력
→ 회원가입 완료
→ 자동 로그인 처리
→ 홈 화면 이동
```

필수 약관:

- 이용약관
- 개인정보 처리방침
- 위치기반 서비스 이용약관
- 신청 유의사항

필수 입력 정보:

- 아이디
- 비밀번호
- 이름
- 전화번호
- 닉네임
- 지역

선택 입력 정보:

- 프로필 사진
- 생년월일

### 8.3 카카오 로그인 흐름

```text
카카오 로그인 클릭
→ 카카오 인증 성공
→ 기존 회원 여부 확인
→ 기존 회원이면 홈 화면 이동
→ 신규 회원이면 추가 정보 입력 화면 이동
→ 약관 동의 및 지역/닉네임 입력
→ 가입 완료
→ 홈 화면 이동
```

### 8.4 개인 플로깅 흐름

```text
개인 플로깅 탭 클릭
→ 로그인 여부 확인
→ 위치 권한 확인
→ 현재 위치 지도 표시
→ 플로깅 시작 버튼 클릭
→ 플로깅 세션 생성
→ 이동 경로 기록 시작
→ 쓰레기 등록
→ 플로깅 종료
→ 활동 결과 요약
→ 저장
→ 커뮤니티 공유 여부 선택
```

활동 결과 요약 정보:

- 활동 시간
- 이동 거리
- 수거한 쓰레기 개수
- 쓰레기 종류별 개수
- 활동 지역
- 활동 날짜

### 8.5 단체 플로깅 참여 흐름

```text
단체 플로깅 탭 클릭
→ 단체 플로깅 목록 조회
→ 지역/날짜 검색
→ 상세 화면 진입
→ 참여하기 클릭
→ 로그인 여부 확인
→ 정원 및 모집 상태 확인
→ 참여 완료
```

### 8.6 단체 플로깅 생성 흐름

```text
단체 플로깅 생성 클릭
→ 로그인 여부 확인
→ 활동 정보 입력
→ 출발지 선택
→ 모집 인원 설정
→ 상세 설명 작성
→ 생성 완료
→ 단체 플로깅 목록에 표시
```

### 8.7 쓰레기 등록 흐름

```text
플로깅 진행 중 쓰레기 등록 클릭
→ 쓰레기 종류 선택
→ 개수 입력
→ 사진 첨부 여부 선택
→ 현재 위치 저장
→ 해당 플로깅 세션에 쓰레기 기록 연결
```

쓰레기 종류 예시:

- 플라스틱
- 캔
- 유리
- 종이
- 비닐
- 일반 쓰레기
- 음식물 쓰레기

### 8.8 커뮤니티 공유 흐름

```text
플로깅 종료
→ 결과 요약 확인
→ 커뮤니티 공유 선택
→ 후기 작성
→ 사진 첨부
→ 공개 지역 선택
→ 게시글 등록
```

### 8.9 내 활동 기록 확인 흐름

```text
마이페이지 진입
→ 내 활동 기록 선택
→ 개인/단체 플로깅 기록 목록 확인
→ 특정 기록 상세 보기
→ 이동 거리, 활동 시간, 쓰레기 수거량 확인
```

### 8.10 데이터 시각화 흐름

```text
마이페이지 진입
→ 내 통계 선택
→ 개인 활동 통계 확인
→ 전체 통계 화면에서 지역별/월별/종류별 통계 확인
```

---

## 9. 데이터베이스 테이블 초안

### 9.1 users

사용자 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 사용자 ID |
| login_id | 로그인 아이디 |
| password_hash | 암호화된 비밀번호 |
| provider | local, kakao |
| name | 이름 |
| phone | 전화번호 |
| nickname | 닉네임 |
| region | 지역 |
| profile_image_url | 프로필 사진 URL |
| birth_date | 생년월일 |
| role | USER, ADMIN |
| created_at | 가입일 |
| updated_at | 수정일 |

### 9.2 terms_agreements

약관 동의 기록을 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 약관 동의 ID |
| user_id | 사용자 ID |
| terms_type | 이용약관, 개인정보 처리방침 등 |
| agreed | 동의 여부 |
| agreed_at | 동의 시각 |

### 9.3 plogging_sessions

개인 및 단체 플로깅 활동 기록을 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 플로깅 세션 ID |
| user_id | 사용자 ID |
| group_event_id | 단체 플로깅 ID, 개인이면 null |
| type | PERSONAL, GROUP |
| status | IN_PROGRESS, PAUSED, COMPLETED, CANCELED |
| start_time | 시작 시간 |
| end_time | 종료 시간 |
| distance | 이동 거리 |
| duration | 활동 시간 |
| trash_count | 쓰레기 총 개수 |
| start_lat | 시작 위도 |
| start_lng | 시작 경도 |
| end_lat | 종료 위도 |
| end_lng | 종료 경도 |
| created_at | 생성일 |

### 9.4 plogging_routes

플로깅 이동 경로를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 경로 ID |
| session_id | 플로깅 세션 ID |
| lat | 위도 |
| lng | 경도 |
| recorded_at | 기록 시간 |

경로 데이터는 5초 또는 10초 단위로 저장하여 데이터가 과도하게 늘어나는 것을 방지한다.

### 9.5 trash_records

수거한 쓰레기 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 쓰레기 기록 ID |
| session_id | 플로깅 세션 ID |
| user_id | 사용자 ID |
| trash_type | 쓰레기 종류 |
| count | 개수 |
| image_url | 사진 URL |
| lat | 위도 |
| lng | 경도 |
| created_at | 등록 시간 |

### 9.6 group_events

단체 플로깅 모임 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 단체 플로깅 ID |
| title | 활동명 |
| leader_id | 인솔자 ID |
| region | 지역 |
| start_time | 시작 시간 |
| end_time | 종료 시간 |
| recruit_deadline | 모집 마감 시간 |
| max_participants | 최대 인원 |
| current_participants | 현재 참여 인원 |
| start_place | 출발지 |
| lat | 출발지 위도 |
| lng | 출발지 경도 |
| description | 상세 설명 |
| supplies | 준비물 |
| status | 모집중, 진행중, 완료, 취소 등 |
| created_at | 생성일 |

### 9.7 group_participants

단체 플로깅 참여자 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 참여 ID |
| group_event_id | 단체 플로깅 ID |
| user_id | 사용자 ID |
| status | JOINED, CANCELED, ATTENDED |
| joined_at | 참여 시간 |

### 9.8 community_posts

커뮤니티 게시글을 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 게시글 ID |
| user_id | 작성자 ID |
| session_id | 연결된 플로깅 세션 ID |
| title | 제목 |
| content | 내용 |
| image_url | 이미지 URL |
| region | 공개 지역 |
| like_count | 좋아요 수 |
| comment_count | 댓글 수 |
| created_at | 작성일 |
| updated_at | 수정일 |

### 9.9 comments

댓글 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 댓글 ID |
| post_id | 게시글 ID |
| user_id | 작성자 ID |
| content | 댓글 내용 |
| created_at | 작성일 |
| updated_at | 수정일 |

### 9.10 post_likes

게시글 좋아요 정보를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 좋아요 ID |
| post_id | 게시글 ID |
| user_id | 사용자 ID |
| created_at | 생성일 |

### 9.11 recycling_spots

분리수거장 또는 공공 쓰레기통 위치를 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 위치 ID |
| name | 장소명 |
| type | RECYCLING_CENTER, TRASH_BIN |
| region | 지역 |
| address | 주소 |
| lat | 위도 |
| lng | 경도 |
| description | 설명 |

### 9.12 notices

공지사항을 저장하는 테이블이다.

| 컬럼명 | 설명 |
|---|---|
| id | 공지사항 ID |
| title | 제목 |
| content | 내용 |
| created_at | 작성일 |

---

## 10. API 목록 초안

### 10.1 인증 API

```text
POST /api/auth/signup
POST /api/auth/login
POST /api/auth/kakao
POST /api/auth/refresh
POST /api/auth/logout
POST /api/auth/phone/send-code
POST /api/auth/phone/verify-code
```

### 10.2 사용자 API

```text
GET /api/users/me
PATCH /api/users/me
GET /api/users/me/activities
GET /api/users/me/statistics
```

### 10.3 개인 플로깅 API

```text
POST /api/plogging/sessions
GET /api/plogging/sessions/{sessionId}
PATCH /api/plogging/sessions/{sessionId}/pause
PATCH /api/plogging/sessions/{sessionId}/resume
PATCH /api/plogging/sessions/{sessionId}/finish
POST /api/plogging/sessions/{sessionId}/routes
GET /api/plogging/sessions/me
```

### 10.4 쓰레기 등록 API

```text
POST /api/trash-records
GET /api/trash-records
GET /api/trash-records/map
GET /api/trash-records/session/{sessionId}
```

### 10.5 단체 플로깅 API

```text
GET /api/group-events
POST /api/group-events
GET /api/group-events/{eventId}
PATCH /api/group-events/{eventId}
POST /api/group-events/{eventId}/join
DELETE /api/group-events/{eventId}/join
PATCH /api/group-events/{eventId}/cancel
GET /api/group-events/me/joined
GET /api/group-events/me/created
```

### 10.6 커뮤니티 API

```text
GET /api/posts
POST /api/posts
GET /api/posts/{postId}
PATCH /api/posts/{postId}
DELETE /api/posts/{postId}
POST /api/posts/{postId}/likes
DELETE /api/posts/{postId}/likes
POST /api/posts/{postId}/comments
GET /api/posts/{postId}/comments
DELETE /api/comments/{commentId}
```

### 10.7 지도 API

```text
GET /api/map/group-events
GET /api/map/trash-records
GET /api/map/recycling-spots
```

### 10.8 통계 API

```text
GET /api/statistics/me
GET /api/statistics/regions
GET /api/statistics/trash-types
GET /api/statistics/monthly
GET /api/statistics/group-events
```

### 10.9 공지사항 API

```text
GET /api/notices
GET /api/notices/{noticeId}
POST /api/notices
PATCH /api/notices/{noticeId}
DELETE /api/notices/{noticeId}
```

---

## 11. 주요 예외 상황

### 11.1 로그인 관련 예외

| 예외 상황 | 처리 방식 |
|---|---|
| 토큰 만료 | refresh token으로 재발급 |
| refresh token 만료 | 로그인 화면 이동 |
| 비로그인 사용자가 참여 버튼 클릭 | 로그인 화면 이동 |
| 카카오 신규 사용자 | 추가 정보 입력 화면 이동 |
| 아이디 중복 | 중복 안내 메시지 표시 |
| 닉네임 중복 | 중복 안내 메시지 표시 |

### 11.2 위치 관련 예외

| 예외 상황 | 처리 방식 |
|---|---|
| 위치 권한 거부 | 권한 허용 안내 화면 표시 |
| GPS 꺼짐 | 위치 서비스 켜기 요청 |
| 위치 정확도 낮음 | 대략적인 위치로 기록 |
| 네트워크 끊김 | 로컬 임시 저장 후 재전송 |

### 11.3 개인 플로깅 관련 예외

| 예외 상황 | 처리 방식 |
|---|---|
| 이미 진행 중인 플로깅 존재 | 이어하기 또는 종료 선택 |
| 쓰레기 등록 없이 종료 | 활동 기록으로만 저장하거나 완료 인정 제외 |
| 앱 강제 종료 | 진행 중 세션 복구 |
| 너무 짧은 활동 | 유효하지 않은 활동으로 처리 가능 |

추천 정책:

> 3분 미만이거나 쓰레기 등록이 0개인 경우, 플로깅 완료 횟수에는 포함하지 않고 활동 기록으로만 저장한다.

### 11.4 단체 플로깅 관련 예외

| 예외 상황 | 처리 방식 |
|---|---|
| 정원 초과 | 참여 불가 |
| 이미 참여한 모임 | 중복 참여 불가 |
| 모집 마감 | 참여 버튼 비활성화 |
| 인솔자가 취소 | 참여자에게 취소 안내 |
| 참여자가 취소 | 참여 상태를 CANCELED로 변경 |
| 완료된 모임 참여 시도 | 참여 불가 |

### 11.5 커뮤니티 관련 예외

| 예외 상황 | 처리 방식 |
|---|---|
| 비회원 글쓰기 | 로그인 요청 |
| 남의 글 수정 | 불가 |
| 남의 글 삭제 | 불가 |
| 부적절한 글 | 신고 기능 제공 |
| 위치 노출 위험 | 지역 단위만 공개 |

---

## 12. 개발 우선순위

### 12.1 1차 개발 목표

1. Flutter 프로젝트 생성
2. Spring Boot 백엔드 프로젝트 생성
3. PostgreSQL 연결
4. 회원가입/로그인 구현
5. 홈 화면 구현
6. 하단 네비게이션 구현
7. 개인 플로깅 시작/종료 구현
8. 쓰레기 등록 구현
9. 내 활동 기록 조회 구현

### 12.2 2차 개발 목표

1. 단체 플로깅 목록 조회
2. 단체 플로깅 생성
3. 단체 플로깅 참여
4. 지도 화면 고도화
5. 분리수거장 위치 표시

### 12.3 3차 개발 목표

1. 커뮤니티 게시글 작성
2. 댓글 및 좋아요
3. 내 통계 화면
4. 지역별 통계 화면
5. 공지사항

### 12.4 4차 개발 목표

1. 카카오 로그인
2. 알림 기능
3. 실시간 채팅
4. AI 쓰레기 분류
5. 관리자 페이지

---

## 13. 최종 정리

이 앱의 핵심 구조는 다음과 같다.

```text
Flutter 앱
→ 백엔드 API 서버
→ PostgreSQL 데이터베이스
```

사용자 흐름은 다음과 같다.

```text
앱 실행
→ 자동 로그인 확인
→ 비회원 홈 또는 로그인 홈 진입
→ 오늘의 플로깅/일정/지역 검색 확인
→ 개인 플로깅 또는 단체 플로깅 선택
→ 위치 권한 확인
→ 플로깅 시작
→ 쓰레기 등록
→ 플로깅 종료
→ 활동 결과 저장
→ 커뮤니티 공유 선택
→ 마이페이지에서 내 활동 기록과 통계 확인
```

현업 기준에서 가장 중요한 설계 원칙은 다음과 같다.

1. 조회는 비회원도 가능하게 하고, 기록·참여·작성은 로그인 후 가능하게 한다.
2. 개인 플로깅과 단체 플로깅은 모두 플로깅 세션으로 저장한다.
3. 쓰레기 등록은 반드시 플로깅 세션과 연결한다.
4. 지도에는 다른 사용자의 실시간 위치를 공개하지 않는다.
5. 커뮤니티 공유 시 정확한 위치 대신 지역 단위만 공개한다.
6. 데이터 시각화는 활동 기록과 쓰레기 기록 데이터를 기반으로 만든다.
7. 채팅, AI 분류, 관리자 기능은 MVP 이후 확장 기능으로 둔다.

